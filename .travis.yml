language: python

matrix:
  include:
    - python: "3.7"
    - python: "3.8"
      after_script:
        - bash <(curl -s https://codecov.io/bash)

install:
  - pip install coverage
  - python setup.py install

script:
  - coverage run -m unittest
